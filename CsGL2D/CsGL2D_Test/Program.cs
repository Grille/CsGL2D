using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using CsGL2D;
using System.Drawing;
using System.Windows.Forms;
namespace CsGL2D_Test
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Console.WriteLine("start");
            var form = new Form();
            form.BackColor = Color.Gray;
            GL2D.SetRenderControl(form);

            Console.WriteLine(GL2D.IsRendererReady());
            Random rnd = new Random(10);
            Console.WriteLine("test");
            Texture texture32 = new Texture(32, 32);
            Texture texture64 = new Texture(64,64);
            Texture texture128 = new Texture(128, 128);
            Console.WriteLine(GL2D.IsRendererReady());

            for (int i = 0; i < 200; i++) {
                new Texture((int)(rnd.NextDouble() * 512), (int)(rnd.NextDouble() * 512));

            }
            texture128.Dispose();
            TextureAtlas._DEBUG();



            GL2D.Clear(Color.Red);
            GL2D.SwapBuffers();
            form.Refresh();
            Console.WriteLine(GL2D.IsRendererReady());
            Console.WriteLine(GL2D.GetError());
            Application.Run(form);
            
            //Console.ReadKey();
            */
        }
    }
}
